# Dart VM Example

The full local Spry example.

It demonstrates:

- file routing
- global middleware
- scoped middleware
- scoped error handling
- `hooks.dart`
- static assets

Run it like this:

```bash
cd example/dart_vm
dart pub get
dart run spry serve
```
