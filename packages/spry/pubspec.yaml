name: spry
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.4.3

dependencies:
  http_parser: ^4.1.0
  routingkit: ^1.1.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.8
